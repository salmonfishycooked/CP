#include <bits/stdc++.h>

using i64 = int64_t;

void solve() {

}

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    freopen("e.in", "r", stdin);
    freopen("main.out", "w", stdout);

    solve();

    return 0;
}
